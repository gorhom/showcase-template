import { DarkTheme } from '@react-navigation/native';

export const darkTheme = {
  ...DarkTheme,
  colors: {
    ...DarkTheme.colors,
    secondaryCard: '#222',
    secondaryText: '#8F8F8F',
  },
};
